package com.benoitletondor.easybudgetapp.view;

import org.junit.Test;

import static org.junit.jupiter.api.Assertions.*;

class ExpenseEditActivityTest extends ExpenseEditActivity{

    @Test
    public void Should_edit_expense()
    {
//        ExpenseEditActivity sut = new ExpenseEditActivity();
//        amountEditText.setText("50.00");
//        String amount = amountEditText.getText().toString();
//        double value = 50.00;
//        double value1 = Double.parseDouble(expense);
       // expenseToSave = expense;
       // expenseToSave.setTitle(descriptionEditText.getText().toString());
       // expenseToSave.setAmount(isRevenue ? -value : value);
       // expenseToSave.setDate(date);
    }
}